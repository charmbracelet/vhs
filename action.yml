name: 'VHS'
description: 'Run VHS to render terminal videos'
inputs:
  tape:
    description: 'tape file'
    required: true
    default: 'vhs.tape'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.tape }}
