name: 'VHS'
description: 'Run VHS to render terminal videos'
inputs:
  path:
    description: 'tape file path'
    required: true
    default: 'vhs.tape'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.path }}
